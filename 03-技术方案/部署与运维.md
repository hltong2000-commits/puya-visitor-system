---
tags: [访客系统, 部署, 运维]
created: 2026-08-28
updated: 2026-08-28
type: 部署运维
author: Codex
source_primary: "用户需求与已确认设计"
source_url: "https://puya-visitor-system.vercel.app/"
verified_date: 2026-08-28
data_quality: "内部方案"
---

# 部署与运维

[[00-MOC/访客系统总览|返回总览]]

## 最小部署

- 一台公司服务器或国内云主机。
- 一个企业域名或内网域名，启用 HTTPS。
- Web 服务以非 root 用户运行。
- 数据库文件和备份目录与静态资源分离。

## 日常运维

- 每日自动备份数据库，至少保留最近 7 个备份版本。
- 定期验证备份可恢复，不只检查文件是否存在。
- 监控接口错误率、响应时间、磁盘空间和备份结果。
- 记录应用版本、配置变更和管理员操作。
- 二维码内容保持稳定，切换域名时使用重定向或重新制作二维码。

## 本地开发

本项目本机使用 Node.js 内置 SQLite；云端 PostgreSQL 模式依赖 `pg`。在项目根目录设置 `ADMIN_PASSWORD` 后执行 `npm start`。登记页为 `/`，后台为 `/admin`。生产环境必须设置管理员密码，不应使用开发默认值。前端视觉样式集中在 `public/styles.css`，LOGO 位于 `public/assets/puya-logo.png`。

## Vercel + Supabase/Neon 试用

将代码推送到 GitHub 后，在 Vercel 导入仓库；在 Supabase 或 Neon 创建 PostgreSQL 数据库，将连接串填入 `DATABASE_URL`。Vercel 入口由 `vercel.json` 指向 `api/index.js`。部署完成后先检查 `/api/health`，确认返回 `database: postgres`，再用公网 HTTPS 地址生成二维码。Vercel 函数使用无状态会话，不能依赖本地文件或内存保存业务数据。

### 当前试用环境

- 正式试用域名：<https://puya-visitor-system.vercel.app/>
- 后台入口：<https://puya-visitor-system.vercel.app/admin>
- 数据库：Vercel Neon PostgreSQL 集成。
- 必需环境变量：Neon 自动生成的 `DATABASE_URL`，以及人工设置的 `ADMIN_PASSWORD`。
- 固定二维码：`public/assets/visitor-registration-qr.png`，内容为正式试用域名。
- 2026-08-28 联调结果：健康接口返回 `database: postgres`；登记页提交返回 HTTP 201 并显示登记成功；后台页面可加载，未登录访问查询接口返回 HTTP 401；使用管理员账号登录后可查询到“联调测试 / PUYA系统联调”记录。
- 联调产生一条姓名为“联调测试”、公司为“PUYA系统联调”的测试记录，前台登录后台确认可见后可将其隐藏。

## 故障处置

提交失败：前台保留纸质应急登记表；系统恢复后补录并标记来源。数据库异常：暂停后台写入，先保护现场数据和备份，再恢复服务。
