NODE_ENV=development
PORT=3000
ADMIN_PASSWORD=replace-with-a-strong-password
# Set DATABASE_URL to switch from local SQLite to PostgreSQL.
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require
